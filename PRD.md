# 产品需求文档 (PRD) - 3038 网关 (Home Assistant 接管小米中枢)

| 项目名称 | 3038-网关-ha (Home Assistant 智能家居中枢) |
| :--- | :--- |
| **版本** | v1.0.0 |
| **创建日期** | 2026-09-16 |
| **状态** | 规划与设计 (Planning) |
| **负责人** | Kavin |
| **目标环境** | PVE 10 宿主机 / KVM HAOS 虚拟机 (VMID `138`, IP `10.10.10.38:8123` / 端口代号 3038) |
| **核心定位** | 逻辑控制中心全面替代小米中枢任务编排；小米中枢仅保留作为射频接收器 (BLE Mesh/Zigbee 基站) |

---

## 1. 项目背景与需求痛点

### 1.1 现状与痛点
* **现有方案**：小米中枢网关负责全屋开关、插座等子设备的通信与自动化编排（结合米家 App / 极客版）。
* **核心痛点**：
  1. **自动化表达能力有限**：米家仅支持最基础的 `IF-THEN`，缺乏复杂条件判断（如状态保持时长、防抖、多传感器状态与/或逻辑、跨维度判定）。
  2. **多规则冗余分散**：一个按键的“开”、“关”、“长按”，在米家中通常需要拆成 3~5 条独立规则，难以维护和排查。
  3. **生态封闭**：无法跨品牌无缝联动（如配合 PVE 上的软路由网络状态、NAS 状态、非米家设备协同联动）。
  4. **功率插座联动痛点**：米家无法精准实现“功率降至 X 瓦且**持续 5 分钟**后断电”，极易在用电器待机波动期间误断电。

### 1.2 改造目标与核心决策
* **逻辑控制替代，物理硬件共存（方案 A）**：
  * **主控大脑**：在 PVE 10 上部署标准 **Home Assistant Operating System (HAOS)**，承担全屋状态监听、逻辑判断、任务编排、能源统计与可视化仪表盘。
  * **信号基站**：保留现存小米中枢网关（以及多模网关/蓝牙网关），**仅作为射频（Bluetooth Mesh / Zigbee）接收器**，为开关插座提供无线链路，不参与任务编排。
  * **通信链路**：HA 通过局域网（LAN）下发控制指令给小米网关或 Wi-Fi 设备，实现亚秒级本地响应，断网不影响局域网控制。

---

## 2. 系统拓扑与架构设计

### 2.1 整体架构拓扑
```text
┌────────────────────────────────────────────────────────────────────────┐
│                          PVE 10 虚拟化宿主机                           │
│                                                                        │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │               【新】Home Assistant 虚拟机 (HAOS)                │   │
│   │           - 全局状态总线 (Event Bus & State Machine)           │   │
│   │           - 任务编排引擎 (Automations / Scripts / Node-RED)     │   │
│   │           - 能源看板与历史记录 (Energy & Recorder)              │   │
│   │           - 集成插件: Xiaomi Miot Auto (优先 LAN 本地通信)      │   │
│   └───────────────────────────────┬────────────────────────────────┘   │
└───────────────────────────────────┼────────────────────────────────────┘
                                    │ 局域网控制 (LAN RPC / Local Token)
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                         家庭局域网 (10.10.10.0/24)                     │
│                                                                        │
│   ┌──────────────────────────────────┐  ┌──────────────────────────┐   │
│   │     Wi-Fi 智能插座 / 智能开关    │  │       小米中枢网关       │   │
│   │      (直连路由器，HA 直接接管)   │  │   (关闭米家自动化编排)   │   │
│   └──────────────────────────────────┘  └─────────────┬────────────┘   │
└───────────────────────────────────────────────────────┼────────────────┘
                                                        │ 蓝牙 Mesh / Zigbee
                                                        ▼
                                          ┌──────────────────────────┐
                                          │ 蓝牙 Mesh 墙壁开关/插座   │
                                          │ (零火/单火开关、人体传感)│
                                          └──────────────────────────┘
```

### 2.2 通信方案选型对比

| 方案 | 控制逻辑 | 射频网关 | 优缺点分析 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| **方案 A<br>(本项目采用)** | **HA 统一编排** | **保留小米中枢** | **优点**：无需采购额外 USB 射频棒，开关零重新配对，蓝牙 Mesh 极度稳定，米家 App 留作应急备份。<br>**缺点**：中枢硬件仍需插电。 | **强烈推荐 (采纳)** |
| **方案 B<br>(物理拔除中枢)** | HA 统一编排 | PVE 外接 USB 协调器 | **优点**：彻底脱离小米硬件。<br>**缺点**：PVE 需 USB 穿透；BLE Mesh 接入繁杂；需要重配全屋设备；风险高。 | 放弃 |

---

## 3. PVE 虚拟机安装规范与配置要求 (重要避坑)

为保证长期 7x24 小时运行稳定，必须严格执行以下 PVE 部署规格：

### 3.1 载体选型与规格
* **形态**：**必须创建为 KVM 独立虚拟机，安装官方 HAOS (Home Assistant OS)**。
  * *绝对禁止*：不要采用 LXC 容器或裸 Docker 跑 HA Core（缺少 Supervisor 无法直接安装官方 Add-on 插件，后期维护极高）。
* **硬件规格配置**：
  * **VM ID / 名称**：指定为 `138`，名称命名为 `haos-138`（或 `haos`）。
  * **BIOS**：**必须选择 `OVMF (UEFI)`**（HAOS 官方 qcow2 镜像仅支持 UEFI 引导，选 SeaBIOS 会开机卡死）。
  * **EFI 存储**：勾选添加 EFI 磁盘，存储于 `local-lvm` 或 SSD 存储池。
  * **机型 (Machine)**：`q35`。
  * **CPU**：2 vCPU，CPU 类型选择 `host`（调用宿主机原生指令集）。
  * **内存 (RAM)**：`4096 MB (4GB)`（最低不少于 2GB，4GB 保证 SQLite/Add-on 稳定运行）。
  * **磁盘 (Disk)**：`32GB ~ 64GB`，总线选 `VirtIO Block` 或 `SCSI`（搭配 `VirtIO SCSI Single` 控制器，勾选 `Discard` 以支持 SSD TRIM）。
  * **Guest Agent**：**必须勾选开启 QEMU Guest Agent**（以便在 PVE 界面获取实时 IP 和平滑关机）。

### 3.2 网络配置与静态绑定
* **网卡**：桥接 `vmbr0`，VirtIO (paravirtualized)。
* **网络参数规范**：
  * **IP 地址**：`10.10.10.38/24`（子网掩码 `255.255.255.0`）。
  * **默认网关**：**`10.10.10.15`**（走旁路由，保证境外生态与 GitHub 畅通）。
  * **DNS 服务器**：**`10.10.10.15`**（由旁路由负责无污染 DNS 解析与分流）。
* **静态分配要求**：
  * 虚拟机初次启动后，在主路由后台通过 MAC 地址将其永久绑定静态租约，同时在 HAOS 网络配置中锁定上述网关与 DNS。
  * 严禁依赖不带特定网关的动态 DHCP，防止 IP 漂移或因直连国内出口导致 GitHub/HACS 插件拉取超时。

### 3.3 网络依赖与优化
* **天然解决 GitHub / HACS 访问痛点**：
  * 网关与 DNS 全面指向 `10.10.10.15` 旁路由后，HAOS 首次开机下载 LandingPage 镜像、Supervisor 在线更新、HACS 社区商店直连拉取、以及 Docker Registry (`ghcr.io`) 均可实现全速无阻拉取，彻底规避国内网络常见卡顿避坑项。

---

## 4. 开关插座接管与属性规范

本项目首期重点接管**开关**与**插座**，具体规格要求如下：

### 4.1 集成插件安装
* **核心插件**：`Xiaomi Miot Auto` (通过 HACS 安装)。
* **账号接入配置**：
  * 登录米家账号同步设备。
  * 过滤选项：白名单选择相关开关、插座与网关实体。
  * 通信模式配置：**勾选“优先本地局域网（LAN）”**。

### 4.2 墙壁开关特性接入
1. **继电器通断（Switch）**：
   * 实体形态：`switch.xxx_switch`。
   * 状态响应：双向同步，物理按压与 HA 软件控制保持毫秒级同步。
2. **转无线开关模式（Wireless Button Events）**：
   * 对闲置或场景联动按键，在米家开启“转无线开关”后，HA 的 `Xiaomi Miot Auto` 会监听按键事件（`button_1_single_click`, `double_click`, `long_press` 等）。
   * 统一在 HA 自动化中作为事件触发器（`trigger: event`）。

### 4.3 计量插座特性接入
1. **电源控制**：`switch.xxx_outlet`。
2. **实时功率监测**：`sensor.xxx_power` (单位：W)。
3. **电量统计**：`sensor.xxx_electric_consumption` (单位：kWh)。
4. **能耗看板集成**：将累计电量实体直接接入 HA 原生 **Energy (能源)** 仪表盘。

---

## 5. 小米自动化迁移与重构优化方案

### 5.1 迁移机制（人工提取 + AI 辅助优化）
由于米家云端 API 封闭且与 HA 语法体系不同，无法实现“一键全自动转换”，采用以下平滑迁移机制：
1. **梳理清单**：列出米家 App 或中枢极客版中的核心自动化触发逻辑。
2. **AI 辅助生成 YAML**：利用标准化 Prompt 将米家规则重构为 HA 的标准 `automations.yaml`。
3. **灰度验证**：在 HA 启用该规则，**同步将米家 App 中的同名规则禁用**，观察 24 小时。

### 5.2 规则优化原则 (相比米家的降维打击)

| 场景 | 米家原有痛点 | HA 优化重构方案 |
| :--- | :--- | :--- |
| **功率插座断电**<br>(如洗衣机/热水器) | 功率低于 5W 立即断电，待机波动时误关。 | 加入持续时长判断：`numeric_state: below 5, for: '00:05:00'`，即低于 5W 且持续 5 分钟才关断。 |
| **多键开关多合一** | 单击、双击、长按在米家需要写 3 条规则。 | 使用 HA `Choose (分支选择)`，1 条自动化管理该开关的所有手势动作。 |
| **开灯与关灯合并** | 开灯写一条，关灯写一条，规则膨胀翻倍。 | 合并为同一自动化，根据当前实体状态使用 `toggle` 或条件分支判断。 |
| **防死循环与访客模式** | 机械式执行，家里来人或特殊场景无法临时屏蔽。 | 引入 `input_boolean.guest_mode`（访客模式开关），作为全局条件（Condition）直接熔断关灯。 |

---

## 6. 运维保障与持久化注意事项

### 6.1 数据库写入与 SSD 寿命保护 (Recorder 优化)
* **背景**：小米功率插座每秒都会上报电压、电流微小波动，默认写入本地 SQLite 数据库会导致磁盘空间暴增，加速 SSD 磨损。
* **防护配置**：在 HA 的 `configuration.yaml` 中配置 `recorder` 过滤策略，仅记录开关状态变化，排除高频微小功率波动：
  ```yaml
  recorder:
    commit_interval: 10
    purge_keep_days: 7
    exclude:
      entity_globs:
        - sensor.*_electric_current
        - sensor.*_voltage
  ```

### 6.2 备份与快照策略
1. **HAOS 内置备份**：开启每周自动完整备份（Full Backup），存储在本地或挂载的 NAS / SMB 共享目录。
2. **PVE 宿主机快照**：重大系统升级或插件变更前，在 PVE 控制台对 HAOS VM 打一个内存快照，随时秒级回滚。

### 6.3 避免控制冲突
* **原则**：同一开关插座的自动化控制权必须**唯一定位**。
* 在 HA 调试好某项任务后，必须**进入米家 App 彻底关闭该自动化**，禁止两端同时存在控制逻辑导致“神仙打架”。

### 6.4 外网访问与 Cloudflare Tunnel 反向代理防坑配置
* **外网域名**：`https://ha.808608.xyz`
* **内网反代目标**：`http://10.10.10.38:8123`（Cloudflare Tunnel ID: `808608`）
* **⚠️ 核心避坑点 (HTTP 400 Bad Request)**：
  Home Assistant 默认具备严格的反向代理防护机制，**禁止未授权的外部代理访问**。如果仅在 Cloudflare Tunnel 配置而不修改 HA 配置，访问 `ha.808608.xyz` 会直接报错 `400: Bad Request`。
* **强制配置项**：在 HA 部署完成后，必须在 `/config/configuration.yaml` 中增加以下反代放行声明：
  ```yaml
  http:
    use_x_forwarded_for: true
    trusted_proxies:
      - 127.0.0.1
      - 10.10.10.0/24  # 放行局域网内的 Cloudflare Tunnel 容器/节点 IP
  ```

---

## 7. 实施路线图 (分阶段执行计划)

```text
阶段 1: PVE 部署准备
  ├─ 确认 PVE 存储与网络资源
  └─ 执行部署脚本 / 建立 HAOS 虚拟机 (VMID: 138, IP: 10.10.10.38, UEFI, 4G RAM, 32G SSD)

阶段 2: 基础环境与网络打通
  ├─ 路由器/HAOS 绑定静态 IP (10.10.10.38) 并指定网关与 DNS 走 10.10.10.15 (旁路由)
  ├─ 验证 GitHub / Docker / Supervisor 连通性
  ├─ 安装 HACS 社区商店
  └─ 安装 Xiaomi Miot Auto 插件并验证登录

阶段 3: 设备接入与实体命名规范
  ├─ 过滤并导入全屋开关、插座实体
  ├─ 检查 LAN 本地控制生效情况 (响应延时 < 100ms)
  └─ 规范化实体重命名 (如 switch.living_room_light)

阶段 4: 自动化迁移与优化试跑
  ├─ 梳理米家核心自动化清单
  ├─ 编写优化版 HA YAML 自动化并加载
  └─ 禁用米家对应旧规则，试运行 24-48 小时

阶段 5: 能源看板与长期维护固化
  ├─ 配置 Energy 能源面板
  ├─ 配置 Recorder 数据库持久化保护
  └─ 制定 PVE 定时快照与备份策略
```

---

## 8. 验收标准

1. **虚拟机稳定性**：PVE 下 HAOS 持续运行 7 天无 OOM、无网络断连、QEMU Agent 正常通讯。
2. **控制低延迟**：局域网内下发开关控制，物理动作响应延迟 <= 200 ms。
3. **状态双向同步**：物理按下开关，HA 界面状态实时更新；HA 界面操作，开关物理状态准确响应。
4. **自动化无冲突**：米家旧自动化全部静默，HA 承接的所有开关插座联动按预期执行，无误触发、无循环冲突。
5. **保护策略生效**：插座功率防抖生效，待机波动不误断电；数据库大小增长受控。
